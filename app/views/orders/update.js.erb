$('#pending').html("<%= j render 'orders/list', orders: @restaurant.orders.pending %>");
<% unless @ready %>
  $('#delivered').html("<%= j render 'orders/list', orders: @restaurant.orders.delivered %>");
<% end %>
